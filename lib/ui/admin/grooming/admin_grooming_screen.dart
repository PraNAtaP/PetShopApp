import 'package:flutter/material.dart';

class AdminGroomingScreen extends StatelessWidget {
  const AdminGroomingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Admin Grooming Management', style: TextStyle(fontSize: 24)),
    );
  }
}
